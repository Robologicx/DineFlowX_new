import "package:flutter/material.dart";

class OrderCard extends StatelessWidget {
  const OrderCard({super.key});
  @override
  Widget build(BuildContext context) => Card(child: Text("Order Card"));
}
