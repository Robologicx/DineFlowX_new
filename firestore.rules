rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Development rule: allow app to seed and run.
      // Tighten this before production.
      allow read, write: if true;
    }
  }
}